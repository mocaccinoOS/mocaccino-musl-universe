name: sudo
category: utils
version: "1.9.4"
requires:
- name: zlib
  category: libs
  version: ">=0"
uri:
  - https://www.sudo.ws/sudo/
license: "custom ISC"

description: "Give certain users the ability to run some commands as root"
